# Storefront Assets

Manual Foundry screenshots and hero images live here, grouped by app id:

```text
store-packages/assets/<app-id>/hero.png
store-packages/assets/<app-id>/screenshot-1.png
```

These files are copied into the generated CRIXA repository metadata area and are not installed as application payload files.
